// ConversationState.cs
// Simple enum for what the user is doing in the chat right now.

namespace CyberSecurityBot.Models
{
    public enum ConversationState
    {
        Idle,
        BrowsingCategory,
        InChatQuiz
    }
}
